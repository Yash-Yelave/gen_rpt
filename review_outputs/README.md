# review_outputs/
# All AI review artifacts are stored here.
# This directory is auto-created by the review system.
# Structure per report:
#   review_outputs/<report-id>/
#     review.json           — complete ReviewData
#     review.md             — formatted audit report (Markdown)
#     review.html           — styled audit report (HTML)
#     claims.json           — claim audit with location references
#     findings.json         — all categorised findings
#     scores.json           — dimension scores only
#     audit_manifest.json   — lightweight run summary for CI/downstream tools
